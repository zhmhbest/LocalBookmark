// import Vue from 'vue'
// import Vuex from 'vuex'
import { Vue, Vuex } from '../library'
Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
});
